{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "build:root": "node build.js --base /",
    "build:subdir": "node build.js --base /portfolio/",
    "preview": "npx vite preview --port 4173",
    "verify": "node test-dock-navigation.js"
  },
  "dependencies": {
    "canvas": "^3.2.3",
    "clean-css": "^5.3.3",
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0",
    "pdf-export-images": "^1.2.0",
    "pdfjs-dist": "^6.1.200"
  },
  "devDependencies": {
    "puppeteer": "^25.3.0"
  }
}
