const path=location.pathname;if(path.endsWith('/project-velora-bites.html')||path.endsWith('project-velora-bites.html')){const key='portfolio-launch:veldora-bites:v1';const seen=sessionStorage.getItem(key)==='1';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const soundKey='portfolio-launch-sound';let soundOn=localStorage.getItem(soundKey)!=='off';const root=document.createElement('div');root.className='veldora-launch';root.setAttribute('aria-hidden','true');root.innerHTML=`<div class="veldora-launch__grain"></div><button class="veldora-sound-toggle" type="button">${soundOn?'Sound on':'Sound off'}</button><div class="veldora-launch__shell"><span class="veldora-launch__eyebrow">Luxury ordering sequence</span><h1 class="veldora-launch__title">VELDORA<br>BITES</h1><div class="veldora-launch__menu"><div class="veldora-launch__dish"><strong>Chef's Selection</strong><span>Menu loaded</span></div><div class="veldora-launch__dish"><strong>Seasonal Special</strong><span>Added to cart</span></div><div class="veldora-launch__dish"><strong>Signature Dessert</strong><span>Offer applied</span></div></div><div class="veldora-launch__flow"><div class="veldora-step">Menu</div><div class="veldora-arrow">→</div><div class="veldora-step">Cart</div><div class="veldora-arrow">→</div><div class="veldora-step">Order ready</div></div><div class="veldora-launch__status"><span>Persistent cart <strong>online</strong></span><span>Simulated checkout ready</span></div></div>`;document.body.prepend(root);document.body.classList.add('veldora-launch-active');let ctx;function tone(freq,start,duration,gain=.035,type='sine'){if(!soundOn||reduce)return;ctx=ctx||new(window.AudioContext||window.webkitAudioContext)();const o=ctx.createOscillator(),g=ctx.createGain();o.type=type;o.frequency.setValueAtTime(freq,ctx.currentTime+start);g.gain.setValueAtTime(.0001,ctx.currentTime+start);g.gain.exponentialRampToValueAtTime(gain,ctx.currentTime+start+.015);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+start+duration);o.connect(g).connect(ctx.destination);o.start(ctx.currentTime+start);o.stop(ctx.currentTime+start+duration+.03)}function soundscape(){if(!soundOn||reduce)return;[0,.18,.36].forEach((t,i)=>tone(520+i*90,t,.12,.025,'triangle'));tone(210,.48,.28,.03,'sine');tone(680,.72,.16,.028,'triangle');tone(860,.94,.2,.025,'sine');tone(330,1.12,.42,.045,'sine');tone(660,1.18,.3,.022,'triangle')}const unlock=()=>{soundscape();removeEventListener('pointerdown',unlock);removeEventListener('keydown',unlock)};try{soundscape()}catch{addEventListener('pointerdown',unlock,{once:true});addEventListener('keydown',unlock,{once:true})}root.querySelector('.veldora-sound-toggle').addEventListener('click',e=>{e.stopPropagation();soundOn=!soundOn;localStorage.setItem(soundKey,soundOn?'on':'off');e.currentTarget.textContent=soundOn?'Sound on':'Sound off';if(soundOn)soundscape()});const duration=reduce?180:seen?440:2200;setTimeout(()=>{root.classList.add('is-complete');document.body.classList.remove('veldora-launch-active');sessionStorage.setItem(key,'1');setTimeout(()=>root.remove(),650)},duration)}