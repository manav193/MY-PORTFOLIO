const key='portfolio-launch:arcade-os:v1';const seen=sessionStorage.getItem(key)==='1';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const root=document.createElement('div');root.className=`arcade-launch${seen?' is-fast':''}`;root.setAttribute('aria-hidden','true');root.innerHTML=`<div class="arcade-launch__scan"></div><button class="arcade-launch__sound" type="button">Sound on</button><div class="arcade-launch__cabinet"><div class="arcade-launch__marquee">ARCADE OS</div><div class="arcade-launch__screen"><div class="arcade-launch__boot"><div class="arcade-boot-line"><span>Power relay</span><strong>engaged</strong></div><div class="arcade-boot-line"><span>CRT sync</span><strong>stable</strong></div><div class="arcade-boot-line"><span>Input matrix</span><strong>locked</strong></div><div class="arcade-boot-line"><span>Launcher runtime</span><strong>ready</strong></div></div></div><div class="arcade-launch__controls"><span>Keyboard · pointer · gamepad</span><span class="arcade-launch__start"><i></i>Press start channel ready</span></div></div>`;document.body.prepend(root);document.body.classList.add('arcade-launch-active');const soundButton=root.querySelector('.arcade-launch__sound');let enabled=localStorage.getItem('portfolio-launch-sound')!=='off';soundButton.textContent=enabled?'Sound on':'Sound off';let ctx=null;function tone(freq,start,duration=.08,type='square',gain=.025,endFreq=freq){if(!enabled||reduce)return;ctx??=new(window.AudioContext||window.webkitAudioContext)();const o=ctx.createOscillator(),g=ctx.createGain();o.type=type;o.frequency.setValueAtTime(freq,ctx.currentTime+start);o.frequency.exponentialRampToValueAtTime(Math.max(30,endFreq),ctx.currentTime+start+duration);g.gain.setValueAtTime(.0001,ctx.currentTime+start);g.gain.exponentialRampToValueAtTime(gain,ctx.currentTime+start+.01);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+start+duration);o.connect(g).connect(ctx.destination);o.start(ctx.currentTime+start);o.stop(ctx.currentTime+start+duration+.02)}function play(){if(!enabled||reduce)return;try{ctx??=new(window.AudioContext||window.webkitAudioContext)();ctx.resume();tone(95,0,.1,'square',.04,70);tone(62,.08,.5,'sawtooth',.022,180);[.45,.7,.95,1.2].forEach((t,i)=>tone(330+i*90,t,.055,'square',.022));tone(180,1.42,.16,'sawtooth',.035,80);tone(660,1.48,.14,'sine',.028,990)}catch{}}soundButton.addEventListener('click',e=>{e.stopPropagation();enabled=!enabled;localStorage.setItem('portfolio-launch-sound',enabled?'on':'off');soundButton.textContent=enabled?'Sound on':'Sound off';if(enabled)play()});if(enabled&&!reduce){const unlock=()=>{play();removeEventListener('pointerdown',unlock);removeEventListener('keydown',unlock)};play();addEventListener('pointerdown',unlock,{once:true});addEventListener('keydown',unlock,{once:true})}const duration=reduce?180:seen?440:2200;setTimeout(()=>{root.classList.add('is-complete');document.body.classList.remove('arcade-launch-active');sessionStorage.setItem(key,'1');setTimeout(()=>root.remove(),550)},duration);