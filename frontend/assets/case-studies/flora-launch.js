const params=new URLSearchParams(location.search);if(params.get('id')==='flora-and-flavor'){const key='portfolio-launch:flora:v1';const seen=sessionStorage.getItem(key)==='1';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const soundKey='portfolio-launch:sound';let soundOn=localStorage.getItem(soundKey)!=='off';const root=document.createElement('div');root.className='flora-launch';root.setAttribute('aria-hidden','true');root.innerHTML=`<button class="flora-launch__sound" type="button">${soundOn?'Sound on':'Sound off'}</button><div class="flora-launch__garden"><span class="flora-leaf"></span><span class="flora-leaf"></span><span class="flora-leaf"></span><span class="flora-leaf"></span></div><div class="flora-launch__core"><span class="flora-launch__eyebrow">Seasonal experience loading</span><h1 class="flora-launch__title">Flora & Flavor</h1><div class="flora-launch__plate"></div><div class="flora-launch__status"><i class="flora-launch__bloom"></i>botanical dining ready</div></div>`;document.body.prepend(root);document.body.classList.add('flora-launch-active');const button=root.querySelector('.flora-launch__sound');button.addEventListener('click',()=>{soundOn=!soundOn;localStorage.setItem(soundKey,soundOn?'on':'off');button.textContent=soundOn?'Sound on':'Sound off';if(soundOn)playSound(true)});function playSound(force=false){if(reduce||!soundOn)return;try{const AC=window.AudioContext||window.webkitAudioContext;const ctx=new AC();const master=ctx.createGain();master.gain.setValueAtTime(0.0001,ctx.currentTime);master.gain.exponentialRampToValueAtTime(.13,ctx.currentTime+.04);master.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+2.15);master.connect(ctx.destination);const tone=(freq,start,dur,type='sine',gain=.08)=>{const o=ctx.createOscillator(),g=ctx.createGain();o.type=type;o.frequency.setValueAtTime(freq,ctx.currentTime+start);g.gain.setValueAtTime(.0001,ctx.currentTime+start);g.gain.exponentialRampToValueAtTime(gain,ctx.currentTime+start+.02);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+start+dur);o.connect(g);g.connect(master);o.start(ctx.currentTime+start);o.stop(ctx.currentTime+start+dur+.03)};tone(164,.02,1.7,'sine',.05);tone(246,.18,.9,'triangle',.035);tone(329,.45,.7,'sine',.045);tone(493,.9,.5,'triangle',.05);tone(659,1.35,.45,'sine',.06);tone(392,1.72,.38,'sine',.055);setTimeout(()=>ctx.close().catch(()=>{}),2500)}catch(e){}}if(soundOn&&!reduce){playSound();const unlock=()=>{playSound(true);removeEventListener('pointerdown',unlock);removeEventListener('keydown',unlock)};addEventListener('pointerdown',unlock,{once:true});addEventListener('keydown',unlock,{once:true})}const duration=reduce?180:seen?460:2250;requestAnimationFrame(()=>setTimeout(()=>{root.classList.add('is-complete');document.body.classList.remove('flora-launch-active');sessionStorage.setItem(key,'1');setTimeout(()=>root.remove(),700)},duration));}