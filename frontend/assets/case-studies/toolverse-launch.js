const path=location.pathname;if(path.endsWith('/project-toolverse.html')||path.endsWith('project-toolverse.html')){const key='portfolio-launch:toolverse:v1';const seen=sessionStorage.getItem(key)==='1';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const soundKey='portfolio-launch:sound';let soundOn=localStorage.getItem(soundKey)!=='off';const root=document.createElement('div');root.className='toolverse-launch';root.setAttribute('aria-hidden','true');root.innerHTML=`<div class="toolverse-launch__bg"></div><button class="toolverse-launch__sound" type="button">${soundOn?'Sound on':'Sound off'}</button><div class="toolverse-launch__core"><span class="toolverse-launch__eyebrow">Local utility system</span><h1 class="toolverse-launch__title">ToolVerse</h1><div class="toolverse-launch__grid">${['PDF','Image','Text','Dev','Student','Calc','Privacy','Offline','Search','Convert','Compress','Export'].map(x=>`<div class="toolverse-module">${x}</div>`).join('')}</div><div class="toolverse-launch__status"><span><strong>70+ tools</strong> indexed</span><span>Local processing ready</span></div></div>`;document.body.prepend(root);document.body.classList.add('toolverse-launch-active');const button=root.querySelector('.toolverse-launch__sound');button.addEventListener('click',()=>{soundOn=!soundOn;localStorage.setItem(soundKey,soundOn?'on':'off');button.textContent=soundOn?'Sound on':'Sound off';if(soundOn&&!reduce)playSound();});function playSound(){try{const C=window.AudioContext||window.webkitAudioContext;const ctx=new C();const now=ctx.currentTime;const master=ctx.createGain();master.gain.setValueAtTime(.0001,now);master.gain.exponentialRampToValueAtTime(.13,now+.03);master.gain.exponentialRampToValueAtTime(.0001,now+2.15);master.connect(ctx.destination);const tick=(time,freq,dur=.07,type='square',gain=.035)=>{const o=ctx.createOscillator(),g=ctx.createGain();o.type=type;o.frequency.setValueAtTime(freq,time);g.gain.setValueAtTime(gain,time);g.gain.exponentialRampToValueAtTime(.0001,time+dur);o.connect(g);g.connect(master);o.start(time);o.stop(time+dur)};for(let i=0;i<12;i++)tick(now+.12+i*.075,420+i*34,.055,'square',.022);const sweep=ctx.createOscillator(),sg=ctx.createGain();sweep.type='sine';sweep.frequency.setValueAtTime(130,now+.05);sweep.frequency.exponentialRampToValueAtTime(680,now+1.15);sg.gain.setValueAtTime(.018,now+.05);sg.gain.exponentialRampToValueAtTime(.0001,now+1.2);sweep.connect(sg);sg.connect(master);sweep.start(now+.05);sweep.stop(now+1.2);tick(now+1.28,540,.18,'sine',.045);tick(now+1.46,810,.22,'sine',.05);tick(now+1.72,1080,.3,'triangle',.04);setTimeout(()=>ctx.close(),2400)}catch{}}if(soundOn&&!reduce){const unlock=()=>{playSound();removeEventListener('pointerdown',unlock);removeEventListener('keydown',unlock)};try{playSound()}catch{addEventListener('pointerdown',unlock,{once:true});addEventListener('keydown',unlock,{once:true})}}const duration=reduce?180:seen?440:2200;requestAnimationFrame(()=>setTimeout(()=>{root.classList.add('is-complete');document.body.classList.remove('toolverse-launch-active');sessionStorage.setItem(key,'1');setTimeout(()=>root.remove(),650)},duration));}